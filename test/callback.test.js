// global.window = require('jest');
// //const callback = require('../src/js/controller');

// test('Debe devolver un objeto', done => {
//     function callback(results) {
//         expect(results[i]).toBe('[]');
//         done();
//     }

//     fetchData(callback);

// });